#!/usr/bin/env bash
fpm --verbose -s dir -t deb --url "https://github.com/shortishly/lighthouse" --name $(bin/app) -v $(bin/version) --prefix /opt _rel
package_cloud push shortishly/lighthouse $(bin/app)_$(bin/version)_amd64.deb
